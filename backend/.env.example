# Database connection string
# Use port 5433 if running database in Docker (to avoid conflict with local PostgreSQL)
# Use port 5432 if connecting to local PostgreSQL installation
DATABASE_URL=postgresql://username:password@localhost:5433/the_referee

# Server port
PORT=3000

# Frontend URL for CORS (default: http://localhost:5173)
FRONTEND_URL=http://localhost:5173

# AI Provider API Keys (at least one required for AI features)

# Groq - FREE and FAST (Recommended)
# Get API key from: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# Google Gemini - Free tier (backup)
# Get API key from: https://aistudio.google.com/app/apikey
# GEMINI_API_KEY=your_gemini_api_key_here
